const initialState = {
    pokemons: [],
    isLoading: false
};

export const homeReducers = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}